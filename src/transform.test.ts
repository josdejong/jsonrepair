import { describe, test, expect } from 'vitest'
import { jsonrepairTransform } from './transform'

describe('transform', () => {
  test('it should transform input in chunks', () => {
    // TODO
    // const output: string[] = []
    // const { transform, flush } = createTransform({
    //   onData: (chunk) => {
    //     output.push(chunk)
    //   }
    // })
  })
})
